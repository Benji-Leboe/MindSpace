<!DOCTYPE html>
<html>
  <head>
    <%- include ./partials/_head %>
  </head>
  
  <body>
    <button type="button" class="btn btn-outline-secondary" data-toggle="modal" data-target="#exampleModal" data-whatever="@mdo">Edit Profile</button>
    <div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered" role="document">
        <div id="view-post-modal" class="modal-content bg-dark text-light">
          <div class="modal-header">
            <h5 class="modal-title" id="exampleModalLabel">What would you like to change?</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
          </button>
          </div>
        <div class="modal-body">
            <div id="modal-user-profile" class="container">
              <div class="user-profile">
                <script>
                  function renderUserProfile(profile) {
                  $('.user-profile').append(createUserProfile(profile));
                  };


                  function createUserProfile(profile) {
                  var { username, avatar, bio } = profile;

                  let $profile = $('<div>').addClass('users container-fluid rounded justify-content-between').attr('id', 'profile-container');
                  let $row1 = $('<span>').addClass('row d-flex align-middle')
                  let $avatar = $('<img>').addClass('avatar mt-5 ml-4 rounded-circle').attr('src', avatar);
                  let $name = $('<h1>').addClass('name mt-6 ml-6 text-white align-middle').text(username);
                  
                    
                  let $form = $('<form>').addClass('form-group container-fluid mt-5');
                  let $textarea = $('<textarea rows="5">').addClass('container-fluid col-12').attr('placeholder', bio);
                  let $bio = $('<input type="submit" value="Edit">')
  
                  $profile.append($row1, $form);
                  $row1.append($avatar, $name);
                  $form.append($textarea, $bio);
                  return $profile;
              };
                </script>
              </div>
            </div>    
    </div>
    <div class="modal-footer">
      <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
    </div>
  </div>
</div>
</div>

</body>
</html>



